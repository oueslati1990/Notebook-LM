I want to create a NotebookLM like app, it will contain a backend (.Net core + clean arch), a frontend (react) and a devops (docker-compose). I will start with a core feature : a RAG app : a user creates a project , uploads files related to a specific topic , and gets as a result a summary. I want OpenAI LLM for response generation and Chere for embedding chunks. I will use Postgresql for storing users, projects, files, chunks .. and PgVector for storing embeddings info , Keyclock for Auth.